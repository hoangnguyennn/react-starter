## API Response Types

Ở đây sẽ chứa response type từ backend, không liên quan tới domain models ở frontend.

Repository sẽ đảm nhiệm vai trò adapter, convert data từ backend sang domain models để app có thể hiểu.
